#include <stdio.h>
/**
 * main - Entry point for the program
 *
 * This function is the entry point for the program. It initializes any necessary
 * data structures or variables, executes the main logic of the program, and
 * returns an exit status to the operating system.
 *
 * Return: 0 on success, non-zero on failure
 */
int main(void)
{printf("with proper grammar, but the outcome is a piece of art,\n");
return (0); }
